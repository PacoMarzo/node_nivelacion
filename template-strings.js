const nombre = "Pepe";
const real = "Guillermo";
//cmo seria para concatenar los dos nombres e imprimirlos por consolsa.

const salida = `${nombre} ${real}`;

const salida2 = nombre + " " + real;

console.log(salida);
console.log(salida2);
